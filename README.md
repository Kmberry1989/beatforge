# BeatForge — Auto-Detecting DAW

Visit the site and you'll automatically get the right version:
- **Desktop**: full DAW with mixer, piano roll, WASM plugins, USB MIDI/audio
- **Mobile**: touch sketchpad with QR share

## Structure
- `/` — auto-redirects based on device
- `/desktop/` — BeatForge v7
- `/mobile/` — mobile companion

## Deploy
Push to GitHub main branch — workflow included.
